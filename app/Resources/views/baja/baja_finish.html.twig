{% extends 'layout.html.twig' %}

{% block content %}

    <div class="container">
      <br>
      <a href="{{ path('oficina_index') }}" style="margin-top: 10px;"><i class="fas fa-angle-left"></i> Volver a {{bajas.oficina.nombre}}</a>
      <hr>
        <h2>Baja finalizada</h2>
      <hr>
        <div class="jumbotron" style="padding: 10px;">
          <div class="row justify-content-center" style="margin: 1px;">
          <h5>La baja de artículo/s ha sido realizada con éxito en {{bajas.oficina.nombre}} </h5>
        </div>
          <div id="dataTransfer" class="row justify-content-center">
            <ul>
              <a class="btn btn-dark" href="{{ path('baja_ver_informe', { 'id': bajas.id }) }}" >Ver Informe</a>
            </ul>
        </div>
        </div>
      </div>

{% endblock %}
