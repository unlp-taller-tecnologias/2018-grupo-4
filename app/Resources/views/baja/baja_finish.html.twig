{% extends 'layout.html.twig' %}

{% block content %}

    <div class="container">
      <br>
      <a href="{{ path('oficina_show', { 'id': bajas.oficina.id }) }}" style="margin-top: 10px;"><i class="fas fa-angle-left"></i> Volver a {{bajas.oficina.nombre}}</a>
      <hr>
        <h2>Baja finalizada</h2>
      <hr>
        <div class="alert alert-success" role="alert" style="margin: 40px;">
          <div class="row justify-content-center" style="margin: 1px;">
          <h5>La baja de artículos ha sido realizada con éxito en {{bajas.oficina.nombre}} <i class="far fa-check-circle"></i></h5>

        </div>
          <div id="dataTransfer" class="row justify-content-center">
            <ul>
              <a class="btn btn-info" style="margin-top: 10px;" href="{{ path('baja_ver_informe', { 'id': bajas.id }) }}" >Ver Informe</a>
            </ul>
        </div>
        </div>
      </div>

{% endblock %}
