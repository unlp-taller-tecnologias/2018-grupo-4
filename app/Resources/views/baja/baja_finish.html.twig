{% extends 'layout.html.twig' %}

{% block content %}

    <div class="container">
      <hr>
        <h2>Baja</h2>
      <hr>
        <div class="jumbotron" style="padding: 30px;">
          <h4>La baja ha sido realizada con éxito </h4>
          <div id="dataTransfer" class="row justify-content-center">
            <ul>

              <a class="btn btn-dark" href="{{ path('oficina_index') }}" style="margin-top: 10px;">Volver a listado de oficinas</a>
            </ul>
        </div>
        </div>
          <a class="btn btn-dark" href="{{ path('baja_ver_informe', { 'id': bajas.id }) }}" >Ver Informe</a>
      </div>

{% endblock %}
