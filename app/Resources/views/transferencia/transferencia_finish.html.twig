{% extends 'layout.html.twig' %}

{% block content %}

    <div class="container">
      <br>
      <a href="{{ path('oficina_show', { 'id': transferencias.oficinaOrigen.id }) }}" style="margin-top: 10px;"><i class="fas fa-angle-left"></i> Volver a {{transferencias.oficinaOrigen}}</a>
      <hr>
        <h2>Transferencia finalizada</h2>
      <hr>
        <div class="alert alert-success" role="alert" style="margin: 40px;">
          <div class="row justify-content-center" style="margin: 1px;">
              <h5>La transferencia ha sido realizada con éxito. Desde {{transferencias.oficinaOrigen}} hacia {{transferencias.oficinaDestino}} <i class="far fa-check-circle"></i></h5>
          </div>
          <div id="dataTransfer" class="row justify-content-center">
            <ul>
              <a class="btn btn-info"  href="{{ path('ver_informe', { 'id': transferencias.id }) }}" style="margin-top: 10px;">Ver Informe</a>
              <a class="btn btn-info" href="{{ path('oficina_show', { 'id': transferencias.oficinaDestino.id }) }}" style="margin-top: 10px;">Ir a la oficina destino</a>
            </ul>
        </div>
        </div>
      </div>

{% endblock %}
