{% extends 'base.html.twig' %}

{% block body %}
    <h1>Nuevo Estado</h1>

    {{ form_start(form) }}
        {{ form_widget(form) }}
        <input type="submit" value="Crear" />
    {{ form_end(form) }}

    <ul>
        <li>
            <a href="{{ path('estadoarticulo_index') }}">Listado de Estados</a>
        </li>
    </ul>
{% endblock %}
