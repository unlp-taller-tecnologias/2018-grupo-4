{% extends 'base.html.twig' %}

{% block body %}
    <h1>Condición</h1>

    <table>
        <tbody>
            <tr>
                <th>Nombre</th>
                <td>{{ condicionArticulo.nombre }}</td>
            </tr>
            <tr>
                <th>Habilitado</th>
                <td>{% if condicionArticulo.habilitado %}Si{% else %}No{% endif %}</td>
            </tr>
            <tr>
                <th>Descripcion</th>
                <td>{{ condicionArticulo.descripcion }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('condicionarticulo_index') }}">Listado Condiciones</a>
        </li>
        <li>
            <a href="{{ path('condicionarticulo_edit', { 'id': condicionArticulo.id }) }}">Editar</a>
        </li>
    </ul>
{% endblock %}
